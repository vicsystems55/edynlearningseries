name: edyn_learning_hub
description: "Edyn Learning Hub parent and learner application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Latest Flame release compatible with the installed Dart 3.10 SDK.
  flame: ^1.35.1
  flutter_riverpod: ^3.2.1
  go_router: ^17.5.0
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
